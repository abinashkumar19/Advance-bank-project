apiVersion: v1
kind: Service
metadata:
  name: admin-analytics-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: admin-analytics
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
