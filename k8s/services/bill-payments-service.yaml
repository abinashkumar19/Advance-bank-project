apiVersion: v1
kind: Service
metadata:
  name: bill-payments-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: bill-payments
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
