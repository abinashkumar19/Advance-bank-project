apiVersion: v1
kind: Service
metadata:
  name: insurance-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: insurance
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
