apiVersion: v1
kind: Service
metadata:
  name: webhooks-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: webhooks
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
