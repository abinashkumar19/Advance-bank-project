apiVersion: v1
kind: Service
metadata:
  name: chatbot-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: chatbot
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
