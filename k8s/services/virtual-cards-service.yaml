apiVersion: v1
kind: Service
metadata:
  name: virtual-cards-svc
  namespace: veerabank
spec:
  type: ClusterIP
  selector:
    app: virtual-cards
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
